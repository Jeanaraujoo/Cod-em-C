#include<stdio.h>
#include<conio.h>
int main(void)
{
  float notas[5] = {7, 8, 9.5, 9.9, 5.2};
  // declarando e inicializando o vetor notas
  
  printf("Tabela de notas");
  printf("%.1f",&notas[1]);
   printf("%.1f",&notas[2]);
    printf("%.1f",&notas[3]);
     printf("%.1f",&notas[4]);
      printf("%.1f",&notas[5]);
      
      return 0;
  }
  
  
